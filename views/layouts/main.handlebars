<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-red-400">
  <nav class="flex items-center justify-between flex-wrap bg-gray-900 p-6">
  <div class="flex items-center flex-shrink-0 text-white border-4 border-blue-400 border-double">
  <a href="/" class="font-semibold text-xl tracking-tight">Tech Blog</a>
  </div>
  <ul class="flex justify-between">
    <li class="mr-3">
      <a class="inline-block border border-blue-400 rounded py-0 w-max mt-2 bg-red-400 hover:bg-white-300 text-white" href="/">Home</a>
    </li>
   {{!-- {{#if loggedIn}} --}}
    <li class="mr-3">
      <a class="inline-block border border-blue-400 rounded py-0 w-max mt-2 bg-red-400 hover:bg-white-300 text-white" href="/">Dashboard</a>
    </li>
    <li class="mr-3">
      <a class="inline-block border border-blue-400 rounded py-0 w-max mt-2 bg-red-400 hover:bg-white-300 text-white" href="/">Logout</a>
    </li>
    {{!-- {{/if}} --}}
    </ul>
    </nav>

    <div class="wrapper">
      <main>
        {{{ body }}}
      </main>
      <footer>
      Thanks for visiting!
    </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
   </body>
   </html>